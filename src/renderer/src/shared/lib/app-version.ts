/**
 * Single source of truth pra versão do app no renderer.
 * Atualizado a cada release junto com package.json.
 * Componentes (TopBar, SobreSection) leem daqui — nunca hardcode inline.
 */
export const APP_VERSION = '0.1.6'
export const APP_VERSION_TAG = `v${APP_VERSION}`
